git clone https://github.com/2tomoee-rgb/mm-Indian-taste.git
cd mm-Indian-taste
# Replace .github/workflows/ci.yml with the fixed version
git add .github/workflows/ci.yml
git commit -m "Fix: Upload only HTML files to avoid symlinks and size issues"
git push
